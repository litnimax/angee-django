# Журнал реализации accounting по онтологии Odoo 19

Начало: 2026-09-05. Журнал фиксирует решения, фактически выполненные изменения, проверки и открытые вопросы. План не считается выполненной реализацией.

Исходный анализ: https://gist.github.com/litnimax/7985e3bb4f7a0a829df17e424fdee800

## 001 — Границы работы

**Решение.** Odoo 19 используется как бизнес-онтология и каталог сценариев. Бухгалтерские правила остаются в `/workspace/arpee-angee/addons/arp/accounting`. Доработки общих механизмов Angee выполняются в `/home/paseo/.paseo/worktrees/1s29tjir/exotic-pony`, worktree `/workspace/angee-django`, ветка `astra/invoice-form-contract`. Перенос accounting в angee-django не требуется. Предложение обсуждать такой перенос в исходном анализе отозвано после уточнения пользователя.

**Исходные снимки:** arpee `fa5c444125595e82260d45e2e53d4c7994ed3ce5`; Angee `64f1d1750f7a9f5afbefc3ce3196fa190ee44280`; Odoo `1a13ceeaee12fe5cc50f287c31f217d4be2a2eaf`.

## 002 — Первый пакет

**План.** Сначала получить воспроизводимую сборку и baseline существующих accounting tests, затем исправить регистрацию платежей: остаток вместо total, независимость направления денег от типа контрагента, валидация платежной группы и банковских счетов. Reconciliation graph, posting guards и последующие этапы остаются в очереди исходного плана.

**Владельцы.** PaymentManager владеет регистрацией и блокировками группы; Invoice/JournalItem — задолженностью и ее остатком; Payment — типом контрагента, направлением и своей проводкой; GraphQL — авторизацией и вызовом предметной операции; React — декларацией формы. Django/PostgreSQL владеют транзакциями и constraints, Angee — существующими money/sequence/transitions. Новая зависимость не предполагается.

**Аналоги.** Существующие InvoiceManager.create_document и фабрики sales/purchase; блокировки в JournalItemManager.reconcile; общий GraphQL write backend и текущая PaymentsPage. Не вводить расчет остатков на frontend или параллельную нумерацию.

**Приемка пакета.** Счет 100 → платеж 40 → платеж по умолчанию 60; возвраты по обоим типам контрагентов; отказ на пустом/несовместимом наборе; отсутствие банковского счета не подменяется receivable; повторный/конкурентный запрос не создает непреднамеренную переплату. Проверки должны проверять проводки и остатки, а не только status.

## 003 — Проверка окружения

**Выполнено.** Проверены README, manage.py, settings.yaml, исходные модели и тесты arpee, backend guidelines и локальный skill angee-workspace в Angee. Рабочее дерево arpee чистое; в worktree Angee находится созданный ранее отчет.

**Наблюдение.** В arpee и родительских каталогах, а также в цепочке родителей целевого worktree не найден angee.yaml. В PATH не найдены angee/uv, в обоих checkout Angee отсутствует .venv/bin/python. README описывает dev stack, который в этой среде не подготовлен. Сборка и тесты пока не запускались.

**Открытый вопрос.** Пользователю отправлен запрос расположения рабочего стека либо способа подготовки нового тестового стека. Skill `.agents/skills/angee-workspace/SKILL.md`, раздел Resolve The Controlling Stack Root, требует уточнить инициализацию, если ancestor stack отсутствует. Стек не инициализирован; до ответа продолжается независимая подготовка изменений и тестовых сценариев.

**Состояние реализации.** Бизнес-код пока не изменен. Журнал создан в целевом worktree; его копия публикуется отдельным gist с доступом по ссылке. Следующие записи будут добавляться по мере решений, изменений и проверок.

## 004 — Регрессионные сценарии первого пакета

**Изменено.** В `/workspace/arpee-angee/addons/arp/accounting/tests.py` добавлен `PaymentRegistrationTests` с четырьмя методами:

- После оплаты 40 из 100 второй платеж без явно заданной суммы составляет 60; оба платежа не оставляют несопоставленный остаток.
- Четыре вида invoice/refund проверяются отдельно: направление денег, знак банковской строки, счет receivable/payable и погашение документа.
- Пустой набор дает ValidationError и не создает Payment.
- Отсутствующий банковский счет блокирует posting; сохраняются draft, пустой номер и отсутствие проводки.

**Проверки.** Python 3 `ast.parse` успешно разобрал измененный файл; обнаружены четыре новых метода. `git diff --check` прошел. Django-тесты не запускались; предполагаемые падения на старой реализации не выдаются за выполненный red-run. Тесты используют существующие фикстуры и бизнес-вызовы, не заменяют Django/ORM моками. Модели, schema и UI еще не изменены.

**Публикация.** Новый gist: https://gist.github.com/litnimax/44d67e5f0e656dbce5366693082b88dc — secret, доступ по ссылке. Проверено создание. Журнал обновляется в этом же gist; исходный аналитический gist остается отдельным документом.

**Следующее действие.** Получить расположение/способ подготовки стека, запустить baseline и регрессионные тесты, затем реализовать первый пакет. Вопрос о стеке остается открытым; отсутствие ответа не считается разрешением на инициализацию.

## 005 — Выбран и подготовлен собственный стек

Пользователь разрешил использовать `/home/paseo/.paseo/worktrees/2f3fqf9s/modest-toad`, ветка `modest-toad`, worktree arpee-angee. Angee остается в `exotic-pony` на `astra/invoice-form-contract`. Указанный пользователем colorful-bumblebee в доступной файловой системе не найден; его процессы и файлы не изменялись.

Регрессионные тесты перенесены из `/workspace/arpee-angee` в modest-toad; исходный patch проверен и снят только с прежнего checkout. Созданы project pyproject/uv.lock, локальная ссылка `.angee/framework`, список framework addon/package roots и process-only `angee.yaml`. Python 3.14.7, uv 0.12.10, Angee CLI 0.12.1, process-compose 1.122.0 и pnpm 11.1.3; libmagic из Debian размещена локально в `.angee/tools/sysroot`. База — собственная SQLite `.angee/data/db.sqlite3`. Проверены свободные порты backend 8601, web 5174 и process-compose 8091. Запуск — через `angee dev`, скрипты сервисов лежат в `.angee/`.

Сборка `angee build` прошла. Первые попытки тестирования выявили неверный import label (`accounting.tests` вместо `arp.accounting.tests`) и отсутствие первичных миграций; после штатного provision миграции созданы. Установка pnpm выполнена через Corepack; попытки получить его через Python package manager/предполагаемый GitHub asset не использованы в итоговом окружении.

## 006 — Подтверждены исходные ошибки и реализован первый пакет платежей

Исходный запуск: 21 accounting test, четыре failure и один error в новых регрессионных сценариях, один PostgreSQL-only skip. Подтверждены сумма 100 вместо остатка 60, оба направления refunds, IndexError на пустой группе, отсутствие отказа при пустом банковском счете. Существующие сценарии прошли.

В `arp.accounting` внесены:

- Invoice определяет тип контрагента и направление погашения; Payment хранит отдельный `partner_type` customer/vendor, который определяет receivable/payable независимо от направления денег.
- PaymentManager.register блокирует debt items по pk, затем перечитывает/блокирует документы; порядок item→invoice соответствует обновлению статуса при reconciliation. Сумма выводится из актуальных residuals. Нет повторного расчета через amount_total.
- Проверяются непустая группа, отсутствие дубликатов, persisted/posted документы с контрагентом, одна компания/валюта/контрагент/вид документа, правильные debt items, ненулевой остаток каждого счета, положительная конечная сумма в точности валюты и конфигурация bank/cash журнала.
- Регистрация по счетам ограничена их остатком; переплата не обрезается молча. Для аванса остается отдельный standalone Payment. Смешанные invoice/refund группы не неттируются автоматически, а получают явную ошибку.
- Удален receivable fallback вместо банковского счета и прежний `_reconcile_invoices`, дублировавший вывод счета из направления. Новая валидация принадлежит Payment, resolver остается тонким.
- В GraphQL и общей форме Payment добавлен partner_type. Подготовлены addon-owned миграции Payment и HistoricalPayment: прежним outbound соответствует vendor. Исторические суммы/проводки не меняются; старые ошибочные refunds не исправляются миграцией автоматически.

Промежуточная проверка после миграций: **83 tests accounting/sales/purchase прошли, 6 PostgreSQL-only skipped**. Работа над дополнительными отрицательными и конкурентными сценариями продолжается. Reconciliation graph и полная защита posted records остаются следующими пакетами.

## 007 — Исправления, необходимые для холодного запуска демо

Первый provision выявил None.currency при загрузке строк продаж до post-load hooks компании. Owner fix: `CompanyAccounting.round_amount` принимает явную валюту документа; sales/purchase передают валюту заказа, сохраняя company rounding mode. Это исправляет также неверное округление документа в валюте с другим количеством знаков; отсутствие валюты GL теперь дает доменную ошибку.

Следующий provision выявил, что CRM Team валидирует direct membership до materialization grants. В `angee.resources` (именно exotic-pony) исправлена потеря dependencies при разделении строк и grants: используется существующий EntryGraph, независимые grants по-прежнему следуют за rows, явная зависимость row→grant имеет приоритет. Все операции атомарны; post-load hooks видят grants. CRM-манифест объявляет зависимость Team от membership grants arp.base. Проверки членства не отключались, ресурсы не исключались.

Добавлен framework regression: grant доступен при clean зависимой строки и при post-load hook; неуспешный импорт и dry run откатывают grant/rows; повторная загрузка идемпотентна. Начальная версия теста ошибочно использовала anonymous wildcard вместо auth/user wildcard; исправлена декларация тестового subject. При запуске resource suite также выявлен отдельный сбой старого теста удаления: knowledge signal ожидает RecordBinding в bare test app. Его происхождение и общий baseline еще проверяются.

После исправления порядка **полный provision --demo прошел: 437 создано, 2 обновлено; schema: ok**. Codegen frontend прошел: 22 addons, 2 схемы. Запуск сервисов и окончательные проверки еще выполняются.

## 008 — Проверки платежей, конкуренции и миграций

Расширены отрицательные сценарии: нулевая/отрицательная/не конечная сумма, переплата, лишние знаки валюты; разные компании, контрагенты и виды документов; draft, дубликат и уже оплаченный документ; журнал продаж вместо банковского и журнал чужой компании. Добавлен тест округления по валюте документа, когда валюта компании еще не установлена.

Для тестов отдельно поднят PostgreSQL 17.5 на `127.0.0.1:55441` в `.angee/pg-test`. Docker socket недоступен; использован PostgreSQL binary package из Maven Central (`io.zonky.test`, embedded-postgres-binaries-linux-amd64 17.5.0). Это временная тестовая база, основной стек остается на SQLite. Два потока одновременно регистрируют полную оплату одного счета: ровно один платеж, второй запрос получает ValidationError, остаток нулевой. Вместе с существующей конкурентной нумерацией и сценариями sales/purchase: **89 тестов, все прошли, без skip**.

После этого добавлен migration integration test: настоящие AddField/RunPython выполняются на изолированных legacy-таблицах Payment и HistoricalPayment; inbound получает customer, outbound vendor, суммы 40.25/60.75 сохраняются, повторная применимость выключается. Factory восстанавливает исходного actor после elevated bookkeeping по контракту Angee. Финальная повторная проверка accounting на PostgreSQL: **28 тестов, все прошли**. Лог содержит ошибки dev-only change publisher при DEBUG=False в тестах; dispatch.send_robust не прерывает тесты. Это не проверка доставки production-событий.

Frontend accounting: **2 файла, 5 тестов, все прошли**. Ruff по E/F/I из конфигурации Angee проходит для измененных Python business sources и resource owner; `git diff --check` и синтаксис shell проходят.

## 009 — Полный набор Angee и разбор окружения

Полный `pytest -q`: **1816 passed, 8 failed, 5 skipped** за 959.95 с. Все восемь failure находятся в `tests/test_settings.py`: унаследованный `ANGEE_PROJECT_DIR` указывал на ARPEE и мешал тестовым временным settings.yaml. Это ошибка первоначального способа запуска тестов в данной сессии. После удаления project-specific env весь модуль повторно прошел: **62 passed**. Полный набор второй раз не запускался; результаты полного запуска и адресного повторного не подменяются утверждением об одном полностью зеленом запуске.

Изолированный resource suite ранее выявил LookupError `knowledge.RecordBinding` в `pre_delete`. На чистом detached checkout исходного Angee commit `64f1d175` этот же тест воспроизводит ту же ошибку: `test_stale_ledger_pointer_to_wrong_live_row_is_repaired`, 1 failed. В полном наборе он проходит, то есть это существующая зависимость от порядка/состояния registry тестов. В production signal ничего не меняли и тест не отключали. Новая регрессия порядка row→grant проходит, включая rollback, dry-run, hook и повторный импорт.

## 010 — Живой интерфейс и воспроизводимый запуск

`angee status --json` подтверждает running для django/web. Стек: ARPEE modest-toad, Angee exotic-pony на astra/invoice-form-contract. UI `http://127.0.0.1:5174`; backend `http://127.0.0.1:8601/graphql/console/`; process-compose 8091. В текущем демо проверен вход `admin` / `admin` (это собственная база, не пароли другого агента).

При реальной проверке Playwright обнаружен blank screen: `arp.base/web/tsconfig.json` ссылался на host web на один уровень выше нужного. Исправлен относительный путь. Host tsconfig приведен к compiler contract Angee по `allowImportingTsExtensions`. CSRF для Vite-origin настроен штатным `ANGEE_PUBLIC_ORIGIN` в скрипте стека.

Сервисы теперь запускаются через отслеживаемый `scripts/accounting-dev.sh`, `angee.yaml` ссылается на него; нет обязательной зависимости от рукописных ignored `.angee/run-*.sh`. Добавлена инструкция `docs/accounting-dev.ru.md`. Generated process-compose.yaml/run исключены из git. Python dependency lock и pnpm lock сформированы для выбранных checkout. Framework JS зависимости устанавливаются по его собственному lockfile; ARPEE подключает source packages workspace links. Большой diff pnpm-lock связан с удалением прежних внешних importer paths; это следует учитывать при review инфраструктурной части.

Браузер: успешный login, загружен список продаж с демо; `/accounting/invoices`, `/accounting/payments`, `/accounting/payments/new` открыты. На трех accounting-страницах нет page errors и HTTP>=400; Partner Type виден в списке и форме. На login остаются отдельный 403 фонового изображения из внешнего worktree и отказы console-запросов до аутентификации; accounting после входа работает. Скриншоты и browser logs находятся только в ignored `.angee/browser*`.

**Ограничения и следующий шаг.** Общий frontend typecheck не проходит: устаревшие `sidebar`, `recordPath`, контракт `MutationDialog`, различия resource routes/model slots, а также отдельные React type instances из двух node_modules; recursive typecheck framework fragments также ожидает generated gql в иной topology. Это не скрыто и не объявлено исправленным проверкой только accounting. Frontend compatibility требует отдельного пакета до общего green build. Дальнейшие accounting-пакеты — корректность полного reconciliation graph, unreconcile и усиление immutability posted records — пока не реализованы. Текущий пакет исправляет регистрацию платежа; не означает production-ready accounting и не переписывает исторические ошибочные refunds.

Завершающая проверка этого пакета: `makemigrations --check --dry-run` — No changes detected; оба `git diff --check` и Ruff измененных Python owners проходят. Временный PostgreSQL остановлен после тестов, baseline worktree удален. Основные django/web остаются running. Коммиты/merge не выполнялись: изменения оставлены в двух согласованных worktree для review.

## 011 — Доступ стека по сети

По запросу пользователя backend переведен с `127.0.0.1:8601` на `0.0.0.0:8601`; Vite явно запускается с `--host 0.0.0.0` на 5174. Изменен отслеживаемый `scripts/accounting-dev.sh`, обновлена инструкция, оба сервиса перезапущены через собственный process-compose 8091. Внутренний Vite→Django proxy сохраняет loopback. Проверены listening sockets и HTTP-доступ по сетевому IP окружения.

## 012 — Обновление из litnimax/angee-django

По запросу пользователя выполнен fetch origin и fast-forward ветки astra/invoice-form-contract с `64f1d175` до `1f2b33c8` (origin/main). Обновление затрагивает 296 файлов и включает Django-native composition и новые версии опубликованных зависимостей. Основной checkout /workspace/angee-django не переключался.

Локальный resource patch предварительно сохранен в stash `accounting resource ordering before upstream 1f2b33c8` и восстановлен поверх обновления. Конфликты двух файлов разрешены: сохранены upstream Dataset/read_groups, проверка единой БД через router и перевод номера ошибки в исходную строку; поверх них сохранен dependency order rows/grants и post-load hooks после grants. Локальный regression test сохранен с новой upstream fixture API. Stash оставлен как резервная копия. Ruff и git diff --check проходят; незавершенных git-конфликтов нет. Framework uv sync выполнен.

**Обнаруженная несовместимость запуска.** Попытка сборки текущего ARPEE на новом framework завершается ImproperlyConfigured: accounting.Invoice redeclares parent field 'created_at' from accounting.JournalEntry; use a narrow abstract child/donor with only its contributed fields. Новый composer запрещает прежнюю форму наследования child/donor с унаследованными timestamp fields. Backend после autoreload не отвечает; актуализация git завершена, совместимость работающего стека после обновления НЕ подтверждена. Требуется отдельная адаптация ARPEE к native composition; массовые изменения бизнес-моделей и миграции базы в рамках подтягивания upstream не выполнялись.

Адресный resource suite: 23 passed, 23 setup errors (Related model 'parties.Handle' cannot be resolved в upstream test registry); это не зеленая верификация совмещенного патча. Полный набор после обновления не запускался. Ранее опубликованные результаты проверок относятся к старой базе framework и не переносятся автоматически на 1f2b33c8.

## 013 — Адаптация ARPEE к native composition

Пользователь разрешил адаптацию. В ARPEE materialized Invoice и все same-row donors переведены с AngeeModel на узкие abstract Django models; Invoice сохраняет RebacModelBase для permission Meta. Удален устаревший child_overrides_parent. Бизнес-поля остаются у прежних владельцев, generic timestamps/manager больше не приносятся расширениями.

Resource participants используют штатный ResourceLoadMixin и ровно один super().after_resource_load после своей работы. Demo-only contributors делегируют и при пропуске; ранние выходы seed-логики вынесены в собственные helper-методы. Добавлен integration test install-tier: после пропущенного демо все же создаются accounting journals, sales/purchase sequences, CRM stages/reasons; повторный вызов не дублирует записи.

Обновлены зависимости Hasura/aggregates/REBAC, project addon dependency group пересобран из manifests. Runtime build и schema проходят. Migration diff содержит только AlterModelOptions на VcsBridge, Directory, Organization и Person; столбцы ARPEE не изменились. Перед migrate сохранена SQLite backup .angee/backups/before-native-adaptation.sqlite3; миграции применены. В отдельной пустой SQLite native-cold.sqlite3 прошел полный provision --demo: 437 created, 2 updated. Основная база сохранена.

Первый полный ARPEE backend run на PostgreSQL: 277 tests, 4 errors в Discuss на отсутствующем MessageKind.USER_NOTIFICATION. Удалены два устаревших фильтра (новая enum такого вида не содержит; NOTIFICATION имеет другое назначение и не подставляется вместо него). Повторный Discuss: 21 passed. Native hook regression: 1 passed. Общий framework pytest запущен без ANGEE_PROJECT_DIR и продолжает работу.

Frontend приведен к текущим route href owners, model slot targets, typed MutationDialog.parseValues и TanStack query isFetching; удален устаревший menu sidebar. Host tsconfig задает единую React type identity для связанных checkout. Общий host typecheck и production build проходят. Accounting browser smoke после перезапуска проходит. Компонентные тесты Discuss пока выявляют отдельную проблему двух React runtime в Vitest; диагностика и настройка тестового владельца продолжаются. Финальные результаты будут следующей записью.

## 014 — Итоговые проверки адаптации

**ARPEE:** полный PostgreSQL run — 278 tests, OK (64.689 с), включая новый hook regression и конкурентные accounting/sales/purchase сценарии. Все consumer frontend suites — 81 tests в 23 файлах, passed. Host typecheck и production build проходят. Backend makemigrations --check --dry-run — No changes detected. Ruff измененных Python owners и git diff --check обоих репозиториев проходят.

**Angee:** полный Python run — 1915 passed, 1 failed, 5 skipped (1056.73 с). Единственный failure — новое upstream ожидание вызова hook перед grants в test_native_import_pipeline_rolls_back_all_groups_grants_and_hooks, несовместимое с согласованным hook-after-grants контрактом. Ожидание обновлено; дополнительно инъецирована ошибка hook после успешных grants и проверен rollback rows/M2M/ledger/grants/on_commit. Повторный весь раздел ресурсов с полной collection тестового registry: 48 passed, 1873 deselected. Полный Python набор после изменения только теста второй раз не запускался. Это не следует представлять одним новым полностью зеленым прогоном. При отдельной collection только resource directory отсутствует parties.Handle в upstream test registry; использован `pytest tests addons/angee/resources/tests -k 'test_resources or test_import_pipeline'`, без исключения resource-тестов.

**Frontend test infrastructure:** временные попытки inline всех зависимостей и optimizer с нерезолвящимися прямыми transitive package names не оставлены. Итог: shared Angee Vitest config дедуплицирует React/React DOM/router и обрабатывает logo/Base UI/Floating UI/Lucide через Vite. Discuss test config включает client optimizer с nested dependency specifiers от @angee/ui, в том числе use-sync-external-store shims, чтобы CommonJS peers использовали React тестового host. Дополнительные обходные зависимости не добавлены. Все 17 Discuss frontend tests проходят; весь Angee App frontend suite также прошел — 214 tests, 24 файла. Команда с аргументом config-vitest фактически запустила весь App suite, поэтому фиксируется его реальный результат.

**Данные и браузер:** повторный resources load --include-demo на отдельной холодной базе — 0 created, 0 updated, 439 unchanged. После рестарта основной стек сохраняет прежние демо-данные; invoices/payments/sales/purchase/CRM/calendar/discuss открываются. Проверены реальные CRM карточки, события календаря и открытие general-переписки с историей, без page/GraphQL errors в этих сценариях. Проверен свежий вход alice/alice и форма нового invoice. Анонимная login-страница делает console-запросы с отказами до входа; успешный login это не блокирует. Ранее недоступный фон login теперь отдается 200: Vite fs.allow включает ровно root проекта и явно связанный framework worktree.

Оба сервиса снова running на 0.0.0.0:8601 и 0.0.0.0:5174. Файлы остаются в согласованных worktree: ARPEE modest-toad, framework exotic-pony / astra/invoice-form-contract. Merge/commit/push не выполнялись. Следующие accounting-пакеты (reconciliation graph и posted immutability) не включены в эту адаптацию.

## 015 — Промежуточные коммиты и начало исправления сверки

Пользователь поручил фиксировать промежуточные коммиты и продолжить следующий пакет. Сохранена ранее проверенная адаптация: ARPEE `4e75e5a` (`feat: validate accounting payments and adapt ARPEE to native Angee`), Angee `006ad59d` (`fix: preserve resource dependency order and linked frontend test runtime`). В окружении отсутствует Git identity; для этих коммитов явно использован автор Codex <codex@openai.com>, без изменения глобальной конфигурации. Дальше завершенные шаги фиксируются отдельными коммитами. Git push не выполнялся.

Владелец reconciliation остается `arp.accounting.JournalItemManager`: PartialReconcile хранит связи и суммы, FullReconcile обозначает полностью погашенную связанную компоненту, `JournalItem.reconciled` — нулевой остаток отдельной строки. Ориентир Odoo 19: `_reconcile_plan_with_sync`, `_all_reconciled_lines`, `_compute_amount_residual` в `account_move_line.py`; бизнес-правила реализуются независимо в Django. Соседние sales/inventory уже используют Django atomic и упорядоченные row locks; граф сверки принадлежит accounting, общий графовый framework/зависимость не нужны.

Порядок блокировок для reconcile, unreconcile и Payment.register: Account → все связанные JournalItem → Invoice. Блокировка Account удерживает область графа стабильной во время обхода; на PostgreSQL применяется NO KEY UPDATE, чтобы разрешать внешние ключи при независимом posting. Цена решения — последовательное выполнение сверок одного учетного счета. SQLite не доказывает конкурентную безопасность. Старая логика stamp только выбранных нулевых строк заменяется вычислением компонент; unreconcile удаляет только выбранные связи, затем пересчитывает всю затронутую область. Старые full stamps включаются в область, чтобы не оставлять ложных маркеров у непереданных строк. Корректные existing stamps сохраняются при повторном вызове.

План проверки: 100→40+60; отмена через payment item; один платеж на несколько счетов; credit note + payment; разные независимые компоненты; запрет несовместимых ссылок; rollback регистрации и отмены; конкурентные register/unreconcile на PostgreSQL. Историческая база автоматически не переписывается и массовая миграция сверок не запускается. Изменение статусов/маркеров касается только области явно выполняемой операции.

## 016 — Компоненты сверки реализованы

ARPEE commit `2959b40` (`fix(accounting): reconcile complete payment graphs and refresh all affected debt`) содержит новый обход связей, расчет полной сверки и пересчет после отмены, единый порядок блокировок в Payment.register/reconcile/unreconcile и 14 новых регрессионных тестов. Старый `_stamp_full_reconciles` удален. Валидация проверяет счет, компанию записи и счета, валюту строки и документа, posted state и единого контрагента для receivable/payable; сохранена возможность сверять legacy GL/reversal строки, у которых контрагент отсутствует на обеих сторонах. Несохраненные и исчезнувшие выбранные строки дают ValidationError. Черновая нулевая строка не становится погашенной от вызова unreconcile.

Проверки на PostgreSQL: первый адресный прогон — 40 passed. После добавления принудительного пересечения reconcile/unreconcile и валютного сценария весь backend ARPEE — **291 passed**, 66.119 с. После последнего уточнения для draft/non-reconcilable строк повторный полный accounting-набор — **42 passed**, 6.890 с; весь ARPEE после этого уточнения повторно не запускался. Новый конкурентный тест удерживает первую операцию между созданием matches и обновлением итогов, запускает отмену через исторический endpoint и проверяет ее ожидание, затем остаток 40 и корректные статусы всех трех документов. Также проходят два конкурентных Payment.register и гонка register с отменой предыдущего платежа. В логах остаются существующие предупреждения authlib и сообщения dev-only InMemoryChannelLayer при on_commit публикации тестов; результаты тестов выше относятся к реальному exit 0.

Сборка composed runtime проходит; schema --check — ok; makemigrations --check --dry-run — No changes detected. Ruff E/F/I выполнен с конфигурацией framework (line length 120), git diff --check проходит. Первоначальный lint без этой конфигурации сообщал о существующих длинных строках; они не скрывались новой конфигурацией. В этом пакете frontend-код и контракты API не менялись, frontend suites из предыдущей адаптации не повторялись.

Браузерная проверка после завершения autoreload: invoices, payments и new payment открываются с сохраненной авторизованной сессией, без page errors и HTTP>=400. Первый browser smoke пересекся с autoreload и зафиксировал временные 502; он не считается успешным, повторный лог чистый. Backend /auth/csrf/ и frontend / отвечают HTTP 200. Основная SQLite-база и демо-данные не перезаписывались. Временный PostgreSQL после проверок остановлен.

Следующий пакет по плану — замкнуть защиту проведенных документов на update/delete/bulk/nested пути и проверять агрегат проводок перед posting. Это не входит в `2959b40`; исторические некорректные проводки и reconciliation-компоненты не исправлялись массово. Сериализация по Account остается явно принятым ограничением пропускной способности.

Дополнительно новый модуль запущен отдельно на SQLite: **12 passed, 2 skipped** (только PostgreSQL-тесты блокировок), 0.701 с. Это проверяет ветку без row locks для нашего dev-стека, но не заменяет PostgreSQL-доказательство конкуренции. Журнал и анализ сохранены в Git; gist обновляется вместе с завершенными итерациями.

## 017 — Защита проведенного учета и атомарное проведение

По команде пользователя продолжается следующий пакет. Владелец финансовых правил — accounting; общий Angee не изменяется. Инвентаризация: Entry/Invoice/Payment не защищали все write/delete пути; JournalItem.save не вызывался при bulk update/delete, nested removal использовал `_base_manager`, M2M имеет собственную through-таблицу. Соседние messaging/knowledge используют Django lifecycle signals для каскадных side effects, но signals не закрывают bulk update/create through rows. Поэтому модель дает доменные ошибки, а окончательная неизменность строк реализована database triggers через штатную addon-owned Django runtime migration. Новая зависимость или самодельный общий framework не вводится. Odoo-ориентиры: `account_move._check_balanced`, `write`, `unlink`, `_post`; `account_move_line.write/unlink`. Не воспроизводим неподдерживаемые Odoo lock dates/reopen protocol частично.

`LedgerDocumentMixin` и queryset читают сохраненное состояние, отвергают редактирование/удаление закрытого документа, bulk status changes и создание документа сразу posted. Invoice добавляет только исключение для расчетного payment_status; маркеры сверки JournalItem остаются изменяемыми. Audit references created_by/updated_by разрешено обнулять при удалении пользователя; это не изменяет финансовые факты. API delete_guard делегирует модели. Прежний configurable reopen теперь выдает явную ошибку даже при allow overlay: полноценного протокола обратного проведения нет, использовать reversal.

`post()` Entry и Payment владеют atomic вокруг всей transition, включая final save_state, и блокируют сохраненный draft до materialization/numbering. Для native Invoice явно блокируются дочерняя и родительская таблицы. Child totals сохраняются до смены родительского status в той же транзакции, чтобы не редактировать child уже после posting. Проверяются минимум две ненулевые строки, точный баланс, счета компании, валюта документа/строк/компании, точность сумм, принадлежность журнала, соответствие invoice/bill журналу, company payment term/taxes/distribution accounts. Force-state persistence тоже проверяет агрегат и номер. Nullable counterparty старых plain GL/invoice моделей этим пакетом не меняется; полноценная локализация, правила налоговых распределений и zero-document policy требуют следующих этапов.

Новая self-contained миграция `posted_ledger_guards` создает по три триггера на JournalEntry, Invoice, Payment, JournalItem и through taxes (15 всего): INSERT/UPDATE/DELETE. Запрет касается финансовых изменений любого недрафтового документа, новых строк к нему, переноса строк, удаления и изменения M2M. На PostgreSQL child writes берут NO KEY UPDATE на родительской entry и проверяют ее состояние после ожидания, на SQLite действует сериализация записей. Триггеры защищают неизменность ledger, но не подменяют доменный метод построения проводок для произвольного SQL администратора. Удаление триггеров обратной миграцией не переписывает данные. Модуль сначала проверен на обоих backend, затем зарегистрирован и материализован как `accounting.0006_posted_ledger_guards`; после materialization его source digest не менялся.

Первые проверки: 28 прежних accounting tests проходят; затем 14 новых domain/DB guards tests на PostgreSQL. Первый параллельно начатый второй PostgreSQL run столкнулся с занятой test_postgres и не выполнялся; повторные прогоны запускаются последовательно. В новом GraphQL-тесте первоначально не установились триггеры в отдельном TestCase (он подтвердил nested-delete обход); migration fixture перенесена в общий base для обеих test classes, проверка теперь проходит. Новый API test использует реальных company accountant и read-only member: draft edit разрешен бухгалтеру, читателю запрещен; posted header update/delete/nested line removal отклоняются с сохранением данных. До добавления последнего race test: 57 accounting/reconciliation/ledger tests на PostgreSQL, 15 ledger tests на SQLite — passed.

Прежний reconciliation test намеренно создавал исторически поврежденные posted rows. Теперь его fixture временно обращает миграцию в рамках тестовой транзакции, создает legacy corruption и восстанавливает защиту ДО вызова reconcile. Проверки самого reconcile сохранены; обходы не разрешены в продукте. Добавлен PostgreSQL test: posting удерживается после формирования проводок, параллельный bulk insert ожидает родительский lock и получает IntegrityError после commit. Первый полный ARPEE backend run: 308 passed, 70.012 с. После уточнения блокировки native parent выполняется заключительный полный прогон.

Основная SQLite перед migrate сохранена в `.angee/backups/before-ledger-guards.sqlite3`; миграция применена успешно. Schema --check — ok, makemigrations --check --dry-run — No changes detected; Ruff E/F/I и git diff --check проходят. Старые проводки миграцией не исправляются и не перепроводятся. Frontend-код и SDL-контракт не менялись.

## 018 — Пакет защиты завершен и закоммичен

ARPEE commit **`bcf604f`** (`fix(accounting): guard posted ledger writes and make posting atomic`). Заключительный полный backend ARPEE на PostgreSQL после всех изменений — **308 passed, 69.016 с**. Это включает 16 новых ledger tests, прежние payment/reconciliation tests, API проверки company accountant/reader и PostgreSQL posting-vs-bulk-insert race. Последний SQLite ledger run — **15 passed, 1.953 с**, до добавления PostgreSQL-only race и уточнения `of=()` (на SQLite ветка без row locks не менялась). Никакие тесты продукта не отключались; разрешение создавать поврежденные legacy records существует только в fixture с восстановленной защитой до проверки бизнес-операции.

`angee build --check` — ok, миграционная история и source digest согласованы. Сравнение основной SQLite с backup через двусторонний EXCEPT подтверждает отсутствие изменений в JournalEntry, Invoice, Payment, JournalItem, through taxes, PartialReconcile, FullReconcile. Установлены 15 триггеров. PRAGMA integrity_check через Django connection возвращает ok; первый вызов через голый sqlite3 не смог проверить существующую generated MD5 column, поскольку функции Django в таком соединении не зарегистрированы — это устранено использованием штатного соединения, данные не ремонтировались.

Browser smoke с сохраненной сессией: invoices/payments/new payment открываются без page errors, HTTP>=400 и redirect на login. Backend /auth/csrf/ и frontend / — HTTP 200; оба сервиса running. Временный PostgreSQL остановлен. Новые финансовые поля или перестроение защищенных таблиц требуют новой migration защиты; опубликованный/материализованный origin не редактировать. Эта инструкция добавлена в `docs/accounting-dev.ru.md` владельца ARPEE. Git push не выполнялся; изменения сохранены локальными промежуточными коммитами, журнал публикуется в существующий gist.

## 019 — Налоги, первый из двух последовательных шагов

Пользователь запросил налоги и график платежей, затем уточнил «one by one». После прерывания до вопроса «working?» изменений этого пакета еще не было; это явно сообщено. Текущая итерация посвящена налогам, график платежей не изменяется.

Инвентаризация владельцев обнаружила одинаковую ошибку в Invoice, SalesOrderLine и PurchaseOrderLine: последовательный net_of_included на нескольких налогах уменьшал базу повторно. Odoo 19 `_eval_tax_amount_price_included` в `account_tax.py` использует сумму процентов общего batch; `_eval_tax_amount_fixed_amount` считает фиксированный сбор от количества независимо от скидки. Независимая Django-реализация принадлежит TaxQuerySet: price×quantity×(1−discount) = base×(1+sum(included rates)) + included fixed charges. Стратегии TaxCompute предоставляют коэффициент базы и фиксированную часть через included_terms; типы не распознаются по строкам в Invoice/заказах. Старый single-tax net_of_included сохранен как совместимый API, но не используется для расчета наборов. Нелинейная или каскадная стратегия обязана явно расширить контракт; скрытого последовательного fallback нет.

TaxLine хранит результат расчета и округляет документ по политике компании. В per_document сначала округляется общий налог, затем его доли распределяются по строкам в единицах валюты; при равных дробных остатках применяется стабильный порядок строк. Включенная gross-сумма сохраняется: округленная база = округленная введенная сумма минус включенные налоги. Это закрывает и копеечный случай 0.05 с двумя включенными 10%, где независимое округление базы и налогов теряло цент. TaxAmounts реализует настоящее распределение по наибольшему дробному остатку; прежняя коррекция всей разницы в строку с максимальным процентом удалена.

Sales/Purchase используют тот же расчет. При пересчете заказа сохраняются согласованные суммы всех строк и шапки, включая per_document, вместо суммирования независимо округленных previews. Объект сохраняемой строки обновляет свои amount-поля после распределения, чтобы GraphQL/вызывающий код не получали прежний preview. Пример регрессии: включенный 10%, строки 0.03 и 0.04 → налог 0.01, база 0.06, итого 0.07; вторая строка возвращает базу 0.03 и итог 0.04 сразу после save.

Tax.posting_distributions требует для выбранной стороны invoice/refund положительные доли в сумме 100% и явные счета компании документа. Отсутствие распределений, 90/110%, отрицательная доля и пустой счет не маскируются округлением или default account журнала. Проверяется активная сторона распределения; ошибка в неиспользуемой refund-настройке не блокирует обычный invoice, но блокирует refund. Отрицательные ставки/удержания и отрицательные factors не поддерживались прежним nonnegative GL; теперь неподдерживаемая ставка дает явную ValidationError. Это не заявление о полном налоговом parity Odoo или локализации.

Первые адресные проверки — 102 passed на PostgreSQL. После расширения сценариев (14 новых tax tests) полный ARPEE backend — 322 passed, 71.903 с; отдельный tax module на SQLite — 14 passed, 0.586 с. Сценарии: 120→100+10+10 для всех четырех invoice/refund видов, правильные знаки долга, fixed+percent в обоих порядках, quantity/discount, included+excluded, per_line/per_document, валюта с тремя десятичными знаками, распределение округления по счетам, rollback неправильной настройки, одинаковые суммы accounting/sales/purchase. После уточнения возврата округленных amount-полей из save выполняется финальный адресный accounting/sales/purchase прогон.

Новых полей/миграций нет; makemigrations --check — No changes detected, schema --check — ok. Browser invoices/payments/new payment проходит с авторизованной сессией без page errors/HTTP>=400. Framework-код и frontend-код не изменены; существующие проведенные документы не пересчитываются, сохраненные ранее draft/order totals обновляются при обычном save/recompute/post. Общий расчет и ограничения описаны в ARPEE docs/accounting-dev.ru.md.

## 020 — Налоговый шаг завершен

ARPEE commit **`a8d9b13`** (`fix(accounting): compute included taxes jointly and preserve rounded totals`). Финальный адресный accounting/sales/purchase/tax прогон после уточнения возвращаемых из save сумм — **104 passed, 18.675 с**. Предшествующий полный ARPEE прогон — 322 passed; полный набор после изменения только возврата amount-полей не повторялся. SQLite tax module — 14 passed. Ruff E/F/I, git diff --check и angee build --check проходят; миграций и изменений SDL нет. Backend/frontend отвечают HTTP 200; временный PostgreSQL остановлен.

Налоги зафиксированы отдельным шагом согласно «one by one». PaymentTerm/график задолженности не изменялся и остается следующим пакетом. Рабочее дерево ARPEE после коммита чистое; журнал решений публикуется в тот же gist и сохраняется отдельным docs-коммитом Angee. Git push не выполнялся.

## 021 — Partner в списке счетов (2026-09-06)

По запросу пользователя в общий InvoiceWorkbench добавлена декларация Column для partner после номера документа. Используется существующий relation renderer и перевод col.partner. Колонка доступна в Customer Invoices и Vendor Bills, использующих один компонент. Backend, schema и миграции не изменялись.

ARPEE commit **`6b1be0f`** (`feat(accounting): show partner in invoice lists`). Авторизованный browser smoke подтвердил заголовок Partner и заполненные названия контрагентов. Страницы invoices/payments/new payment открылись без page errors и HTTP>=400; git diff --check прошел. Для однострочной декларации новые тесты не добавлялись.

Первая попытка публикации была отклонена автоматической проверкой разрешений. Пользователь затем явно подтвердил публикацию этой записи и результатов проверки в существующий gist 44d67e5f0e656dbce5366693082b88dc сообщением «go».

## 022 — Компактный редактор аналитики (2026-09-06)

Пользователь указал на высокий JSON-редактор analytic_distribution в строке invoice. Проверены локальные исходники Odoo 19: account_move_views.xml использует специализированный analytic_distribution widget (группа аналитики, optional column); шаблон аналитики показывает компактные метки и popover с аналитическими счетами/процентами. Пользователь подтвердил реализацию компактного редактора сообщением «go».

**Владельцы и решения.** Доменный редактор принадлежит arp.analytic; accounting только запрашивает company через штатный returning для контекста формы. Angee передает виджету child row и parentRow, не интерпретируя поля компании. Обнаружен разрыв контракта: frontend регистрировал addon widgets, backend metadata принимал только встроенные имена. Общий контракт расширен квалифицированными именами namespace.addon.widget; неизвестные голые имена и некорректные сегменты по-прежнему отклоняются. Никаких упоминаний аналитики в framework-коде нет.

**Реализация.** Виджет arp.analytic.distribution показывает названия счетов и проценты в компактной ячейке, раскрывает редактор на штатных Popover/Table/RelationField/Input/Button. Apply передает валидное распределение в форму, Save сохраняет документ; Cancel не изменяет форму. Добавление, удаление, очистка, read-only просмотр; положительные проценты до четырех знаков, отдельный итог 100% по каждому плану, запрет повторных и неизвестных счетов. Серверные бизнес-правила остаются окончательными. Авторизованный типизированный GraphQL-запрос разделяет кэш между ячейками; на сохраненном документе выбор ограничен его компанией. Архивные счета сохраняют названия в уже записанных распределениях, но не предлагаются для новых частей. Недоступные счета не удаляются автоматически. Ошибка загрузки показана с Retry; при отсутствии активных счетов — подсказка о настройке. При создании до определения компании используется авторизованный справочник; сервер проверяет принадлежность компании. Глобальный переключатель аналитики, скрытие колонки и правила mandatory/unavailable Odoo этим пакетом не реализуются.

**Данные.** analytic_distribution остается обычным JSONField и прежней картой public id → процент. Добавлена только inert angee_widget metadata. makemigrations --check --dry-run: No changes detected. Старые распределения и проведенные документы не переписывались, materialized migration origins не изменялись.

**Проверки.** Аналитика frontend: 16 passed; accounting frontend: 5 passed. UI Angee: 108 файлов, 726 passed; после уточнения типизации тестовой фикстуры адресные context tests: 5 passed. Backend ARPEE: 42 tests, OK, 2 PostgreSQL-only skipped (SQLite). Framework metadata: 11 passed, 32 deselected. Architecture guardrails: 16 passed. TypeScript приложения и UI framework, production Vite build, Ruff E/F/I и git diff --check проходят. angee build --check и schema --check: ok. Полный backend ARPEE/PostgreSQL заново не запускался для этого UI-пакета.

**Браузер.** На форме существующего счета нет CodeMirror; строка компактная, popover открывается без page errors/GraphQL errors. На отдельном временном draft проверен полный цикл Engineering 60% / Sales 40% → Apply → Save → reload; значения сверены с SQLite. Временный draft затем удален; пользовательские счета не использовались для записи. Frontend по http://192.168.0.90:5174 отвечает HTTP 200.

**Разбор промежуточных ошибок.** Codegen сначала отверг ASC (Hasura enum требует asc); исправлен запрос. Новый Python import был добавлен после первого NameError. Реальные UI tests выявили дублирование React в связанном checkout; применена уже используемая в Discuss конфигурация optimizer без подмены компонентов. Framework pytest сначала не нашел libmagic — повторен с штатным .angee/env.sh. Последняя TypeScript ошибка касалась optional fields в тестовой metadata; fixture уточнена через satisfies. Проверки не отключались.

**Коммиты.** Angee **daae5503** — addon-qualified widgets и контекст документа; ARPEE **eb04c24** — редактор аналитики, запрос, тесты и документация. Git push не выполнялся. PaymentTerm/график задолженности остается следующим бухгалтерским пакетом.

Первая попытка публикации записи 022 была отклонена автоматической проверкой. В ответ на запрос публикации именно этой записи в gist 44d67e5f0e656dbce5366693082b88dc пользователь уточнил: «Я постоянно разрешаю обновлять gist». Это постоянное разрешение обновлять существующий журнал решениями, имплементацией, проверками и коммитами следующих итераций; повторно запрашивать его для обычных записей журнала не требуется.

## 023 — Выравнивание строк и компактный выбор налогов (2026-09-06)

Во время анализа следующего пакета PaymentTerm пользователь указал на наложение полей в invoice lines. До переключения на это исправление график платежей только исследовался, бизнес-код и миграции не менялись.

Причины принадлежат Angee UI: EditableLines задавал auto-треки для пустых краев заголовка и занятых кнопками краев строки, поэтому заголовки не совпадали с ячейками. Все основные колонки сжимались через minmax(0,1fr); NumberField без заданной ширины выходил за пределы ячейки. Many2ManyEdit отдельно выводил метки и кнопку Select вертикально.

В общем EditableLines края получили одинаковые размеры, основные колонки — минимальную ширину, M2M — дополнительное место. Узкая форма прокручивает только область строк; соседние контролы не перекрываются. Float/Integer widgets заполняют ширину своей ячейки. Many2ManyEdit теперь использует существующий SelectPrimitive с multiple: одна строка, метка первого значения и счетчик остальных, полный список с отметками и снятием выбора в popup. Сохраненные id за пределами загруженных options остаются видимыми и удаляемыми. Read-only не получает редактор. Налоговые формулы, API и данные не изменены; эффект наследуют все M2M-поля на этом виджете, включая выбор счетов в регистрации платежа.

Проверки: адресные 7 tests passed; после адаптации двух существующих ActionFormDialog scenarios под новый интерфейс полный UI Angee — 108 файлов, 728 passed. Проверки prefill и пользовательского удаления выбранного счета сохранены. TypeScript UI/приложения, production Vite build и git diff --check проходят. Backend tests не перезапускались: изменение только отображения.

Браузер на реальном invoice: ширины 1280/1920, заголовки совпадают с ячейками, видимые input/button остаются в границах ячеек, горизонтального переполнения всей страницы нет. Высота строки уменьшилась с 78 до 50 px. Tax popup открывается, page errors и HTTP>=400 отсутствуют. Первая проверка видела старый prebundle Vite; frontend перезапущен через process-compose, повторная проверка проходит. Пользовательские счета в браузере не сохранялись. Frontend и backend proxy по 192.168.0.90:5174 возвращают HTTP 200.

Коммит Angee **0b2287f3** — fix(ui): align editable line columns and compact multi-relation inputs. ARPEE worktree не изменен. PaymentTerm/график задолженности остается следующим пакетом. Журнал обновляется в существующем gist на основании постоянного разрешения пользователя.

## 024 — Части задолженности и график оплаты (2026-09-06)

Пользователь разрешил автономное продолжение по цели с журналом решений и промежуточными коммитами. Следующий ранее согласованный пакет PaymentTerm завершен. ARPEE commit **a815316** — feat(accounting): materialize installment debt and settle by maturity.

**Модель и Odoo.** Локальный Odoo 19 account_payment_term._compute_terms распределяет сумму по срокам, округляет части и оставляет остаток последней; account_move_line хранит date_maturity и упорядочивает платежные строки по сроку. В ARPEE существовал compute_due, но Invoice создавал одну общую counterpart. Теперь каждый ненулевой срок материализуется отдельной строкой receivable/payable. Это самостоятельная Django-реализация бизнес-контракта, не перенос Odoo runtime.

**Решения.** Основа графика — invoice_date либо date проводки; без PaymentTerm используется явный due_date либо дата документа. Пустое условие означает немедленную оплату. Проценты/фиксированные суммы положительны, balance один, последний, с amount=0. Без balance явные доли обязаны покрывать сумму до округления. В отличие от неявного исправления произвольной последней доли неверная настройка дает ValidationError и откат всего проведения. Ранние части округляются политикой компании и ограничиваются оставшимся total, последняя получает остаток; одинаковые даты объединяются, нулевые части пропускаются. Налоговый total сохраняется для всех четырех invoice/refund видов. Заголовок due_date — последний фактический срок. Скидки ранней оплаты, календарные условия конца месяца и FX пока не реализованы.

**Погашение.** Уже существующая регистрация платежа выбирает все counterpart выбранных счетов. Распределение теперь сортирует выбранные части после захвата блокировок по maturity, дате проводки, PK. Порядок блокировок PK сохранен. У неизвестного исторического срока используется дата проводки только для порядка; сохраненный NULL не подменяется. Платежная долговая строка получает дату платежа. Частичные платежи/доплата/unreconcile сохраняют корректные остатки и статусы. Изменение справочника условий после проведения не пересчитывает долг. Автоматический reverse invoice сохраняет прежний контракт без переноса payment_term; обычный GL reverse копирует maturity.

**Миграция.** Новый самодостаточный origin installment_maturity материализован как accounting.0007. Добавляет nullable indexed date_maturity и обновляет все 15 DB guards, защищая новое финансовое поле. Замороженный код guards намеренно скопирован в новый origin, прежние модули не изменялись. Проверен forward/reverse на старом состоянии с проведенной строкой в SQLite/PostgreSQL. На рабочей SQLite перед apply создана .angee/data/before-installment-maturity.sqlite3; сравнение всех прежних колонок 15 accounting-таблиц в обе стороны не обнаружило изменений. Исторические maturity NULL, 15 guards, integrity_check=ok. Не было reset/fake/backfill.

**UI/API.** Due date в общей форме Invoice/Bill открывает штатный Popover/Table: сроки, суммы, остатки и валюта. Чтение payment_schedule типизировано, идет при открытии, привязано к инвалидации Invoice/JournalItem/Payment. Draft объясняет появление графика после проведения; неизвестный legacy срок явно подписан, ошибка загрузки скрывает устаревшие суммы и дает Retry. Проверен GraphQL под обычным членом компании (включая кредитовый долг поставщику), чужой пользователь не видит счет. Изменений бизнес-кода framework нет.

**Проверки.** Полный ARPEE backend PostgreSQL — 333 passed, 73.701 с. После добавления API authorization scenario адресный пакет — 12 passed, 3.737 с. Первые 11 сценариев прошли и SQLite. Accounting UI — 3 файла, 9 passed. TypeScript приложения, production Vite build, Ruff по принятой конфигурации framework, diff --check, makemigrations --check, angee build --check, schema --check проходят. Предупреждение о крупном JS chunk существовало до изменения. Браузер на пользовательском draft проверил compact control/popover без записи, page overflow, page errors или HTTP>=400; проведенных документов в рабочей демо-базе нет, реальные installment/payment сценарии проверялись в тестовых БД. Оба сервиса остаются на 0.0.0.0, frontend :5174.

**Промежуточная диагностика.** Первая shell-команда использовала отсутствующий python; заменен на python3. Go angee build строит контейнеры, runtime строится manage.py angee build. Тест отрицательных правил сначала не изолировал ошибку StateField savepoint-ом, тест reverse использовал измененный in-place ProjectState; исправлены фикстуры. API isolation test сначала наследовал системный контекст setup; выделен отдельный класс без elevation для чтения. Сырой sqlite integrity_check не знает Django MD5 generated function — перепроверено штатным Django-соединением. Имя процесса backend в process-compose оказалось django; web успешно перезапущен, Django autoreload подхватил изменения. Эти ошибки не скрывались ослаблением проверок.

Следующий пакет — дата закрытия учетного периода, запрет проведения задним числом и управляемая поверхность настроек. Далее — номера/аудит и бухгалтерские отчеты. Git push не выполнялся. Публикация журнала продолжает постоянное разрешение пользователя «Я постоянно разрешаю обновлять gist».

## 025 — Закрытие периода и датированные исправления (2026-09-06)

Продолжение по постоянному разрешению автономно двигаться к цели. ARPEE commit **37b9108** — feat(accounting): enforce company period locks and dated reversals. Предыдущий пакет графика — **a815316**; запись 024 сохранена Angee docs-коммитом **2a52eff8** и опубликована в gist.

**Онтология и границы.** В локальном Odoo 19 company.py различает global/tax/sales/purchase/hard lock dates; account_move проверяет fiscal locks. Для текущей ступени выбран обратимый общий запрет проведения на дату закрытия включительно и ранее. Необратимый hard lock, налоговые границы, наследование от родительских компаний, персональные исключения и автоматическое смещение даты не добавлены. Они требуют отдельных правил. Новый PeriodLock принадлежит arp.accounting и ссылается на компанию; arp.base не получает бухгалтерских API. Одна политика на компанию, NULL/отсутствие означает открытый учет.

**Настройка и аудит.** Accounting → Configuration → Period Locks показывает компанию, Closed through, причину и изменение. Причина обязательна, штатные AuditMixin/HistoryMixin сохраняют версии обычных model/API записей. Для открытия дата очищается с причиной; удаление политики и перенос между компаниями запрещены, включая прямые ORM обходы. Право настройки имеют company_admin и accounting_admin; accountant видит политику и проводит документы, но не открывает период. Company admin, в свою очередь, не получает право сторно вместо отдельного accountant. История произвольного SQL обслуживания вне model/API не объявляется полноценным DB audit trail.

**Проведение и конкурентность.** Invoice/JournalEntry/Payment читают свежую политику до материализации/получения номера. Новые пять DB triggers дополнительно охраняют переход в posted и запись политики. На PostgreSQL переход берет FOR SHARE на компанию, изменение политики — FOR NO KEY UPDATE. Отдельный последующий запрос политики после ожидания видит committed закрытие, включая гонку с первым INSERT политики. Успешный race test доказывает, что прямой ORM status update ждет незакоммиченную настройку и затем отвергается. Порядок не требует брать блокировки отдельных entry со стороны настройки. SQLite остается последовательным dev backend.

**Исправления и долги.** При просмотре закрытия обнаружился прежний UI-пробел: Invoice date редактировалась, Accounting date — нет; reverse всегда использовал дату источника. Теперь draft Invoice/Bill имеет отдельную Accounting date. Reverse собирает обязательную Reversal date в штатном ActionFormDialog и создает draft на выбранную открытую дату. API date опциональна для совместимости, без нее проверяется прежняя дата источника. Проведение проверяет период повторно, если его закрыли после создания draft. Источник остается неизменным. Долги закрытого периода можно оплачивать платежами открытого периода; reconcile/unreconcile меняют только производные остатки/статусы и не блокируются датой исходного счета.

**Миграции и рабочие данные.** Штатный makemigrations создал PeriodLock/HistoricalPeriodLock (runtime accounting.0008), последующий angee build материализовал самодостаточный period_lock_guards (0009). Предыдущие origins не редактировались. После backup .angee/data/before-period-lock.sqlite3 миграции и rebac sync применены к modest-toad; Django/Vite перезапущены через process-compose. Сравнение прежних колонок 16 таблиц (15 accounting + arp_company) в обе стороны подтверждает отсутствие изменений, пять новых period guards присутствуют, integrity_check=ok. Ни одна политика в рабочей базе не создана, периоды компаний автоматически не закрывались.

**Проверки.** Итоговый полный PostgreSQL backend — **344 passed, 77.165 с**. Адресные period/installment — 22 passed PostgreSQL; SQLite — 22 tests, OK, 1 PostgreSQL-only skipped. Accounting UI — 3 файла, 9 passed. TypeScript приложения, production build, Ruff по принятой конфигурации, diff --check, makemigrations --check, angee build --check и schema --check проходят. Browser проверил list/create Period Locks и Accounting date существующего invoice без сохранений и HTTP>=400/page errors. Новая форма просмотрена визуально, перекрытий нет. Frontend http://192.168.0.90:5174 работает; backend подтвержден реальными GraphQL чтениями браузера.

**Промежуточные сбои.** Первое внесение даты сторно не нашло точную строку исходника; повторено с правильным типизированным объявлением fields. Первый полный прогон: 343 passed, один тест сторно получил отказ прав, поскольку его пользователю назначили company_admin без accountant. Исправлена тестовая роль, продуктовые права не расширялись; адресный и затем полный прогоны зеленые. Browser locator Closed through совпал и с label, и с date button; заменен на доступное имя кнопки. Это была ошибка сценария, не формы.

**Далее.** Завершены график задолженности и общий контроль закрытого периода. Следующие отдельные пакеты: уникальность/политика номеров и аудит исправлений; GL и оборотно-сальдовая ведомость; задолженность и aging на выбранную дату; баланс/P&L. Затем банковские промежуточные счета/выписки/matching и валютные разницы. Локализация и регламентированные формы остаются отдельным этапом. Git push не выполнялся. Журнал публикуется по постоянному разрешению пользователя.

## 026 — Уникальность проведенных номеров (2026-09-06)

По очередному Go завершен контроль идентичности проведенного документа. ARPEE commit **766e64b** — fix(accounting): enforce posted number uniqueness and payment identity.

**Политика и Odoo.** Локальный Odoo 19 account_move.py определяет partial unique index по name/journal_id для posted, исключая служебный placeholder /. В ARPEE проведенный JournalEntry уникален по company/journal/number, Payment — по company/number. Компания явно входит в ограничение. Draft не резервирует номер; отдельного slash-placeholder нет. Проведенный номер непустой, без краевых пробелов, не длиннее 64 символов. Payment имеет тот же номер, что его entry. Выделены понятные model validation errors и независимые ограничения БД.

**Транзакции.** Ручной конфликт, ошибочный постоянный шаблон последовательности или совпадение Payment с номером другого платежа отменяют проведение, новые строки и продвижение счетчика. Автоматический пропуск конфликтующих значений и молчаливая перенумерация не добавлены. Годовой формат и корректный шаблон остаются настройкой последовательности. Проверена гонка ручного номера на PostgreSQL: выигрывает только одно проведение.

**Миграция и данные.** Новый замороженный origin posted_number_uniqueness (runtime accounting.0010) сначала проверяет исторические дубликаты, формат номера и совпадение payment/entry. Затем создает два partial unique index и два UPDATE guard. Прежние triggers сохранены; INSERT сразу posted уже запрещен ledger guards. SQLite не перестраивает таблицы. Проверен forward/reverse на изолированной схеме обеих СУБД. Перед apply создан .angee/data/before-number-guards.sqlite3; сравнение 18 прежних таблиц подтвердило отсутствие изменений, integrity_check=ok, оба новых guard присутствуют. Периоды остаются открытыми.

**Проверки.** Адресный PostgreSQL пакет numbers/period/ledger/accounting — 62 passed, 13.127 с. SQLite numbers/period — 18 tests OK, 2 PostgreSQL-only skipped. Ruff, diff --check, makemigrations --check проходят. Общая регрессия запускается вместе со следующим пакетом ОСВ. Тест period-lock raw update теперь задает валидный номер, чтобы попасть именно в period guard. После rollback тест ручного повтора обновляет Django-объект из БД: транзакция не восстанавливает поля экземпляра в памяти. Изолированный migration test использует собственные имена индексов, чтобы не конфликтовать с живой схемой; production origin не менялся после материализации.

**Граница пакета.** Хронологическая непрерывность нумерации, аудит административного изменения последовательностей и регламентированные правила конкретной страны еще не реализованы. Следом — ОСВ по проведенным фактам с проверкой доступа к полной компании; далее GL и aging. Git push не выполнялся. Запись публикуется по постоянному разрешению пользователя обновлять этот gist.

## 027 — Оборотно-сальдовая ведомость и точный CSV (2026-09-06)

ARPEE commit **785e9d7** — feat(accounting): add authorized trial balance and exact CSV export. Предыдущий контроль номеров **766e64b** записан docs-коммитом Angee **370b1947**, запись 026 опубликована в gist. Пакеты выполняются последовательно по разрешению автономно двигаться к цели.

**Онтология и расчет.** Локальный Odoo 19 addons/account/models/account_report.py:601 различает from_beginning, to_beginning_of_period, strict_range и fiscal-year scopes. Здесь реализован конкретный накопительный trial balance по проведенному ledger, а не весь движок Odoo account.report. Company и включительный диапазон accounting dates обязательны. Начальное сальдо включает все даты ранее начала, дебет/кредит оборота — даты внутри периода, конечное сальдо — начальное плюс дебет минус кредит. Сальдо разделяется на дебет/кредит по счету. Архивные счета с остатком/оборотами включены, пустые опускаются; движения с нулевым чистым результатом сохраняются. Draft/cancelled/future исключены. Платежи и возвраты включаются через свои проводки. Накопление продолжается через начало года; автоматический перенос результата на retained earnings и закрытие P&L не подразумеваются.

**Права и целостность.** Owner reports.trial_balance допускает company member/accountant. GraphQL сначала требует сессию и видимую компанию, затем owner проверяет право на полный отчет. После проверки ограниченный system_context читает строго выбранную компанию: частично видимый ledger нельзя выдавать за полную отчетность. Строки агрегируются одним SQL запросом, итоги выводятся из этих же строк, три пары дебет/кредит проверяются на равенство. Чужой/отсутствующий account и несовпадение валюты проводок с текущей валютой компании вызывают отказ. Это предотвращает молчаливую переподпись исторических сумм после смены валюты. Конвертации валют пока нет.

**Контракт и UI.** Pydantic один раз владеет shapes amounts/row/report, Strawberry pydantic_node публикует типизированный parameterized snapshot, codegen дает типы React. Неперсистентный запрос отчета не имитирует CRUD ресурс или фильтры Hasura для передачи параметров; выбран существующий в framework подход authored query + RowsListView, как для activity agenda. Business-код остается в ARPEE. Accounting → Trial Balance, URL /accounting/trial-balance. ActionFormDialog задает компанию и From/To, MetricStrip показывает шесть итогов, RowsListView управляет таблицей/локальным поиском/пагинацией. Смена параметров не показывает snapshot другой компании; ошибка скрывает прежние суммы. Есть Refresh и Export full CSV. Итоги и экспорт относятся ко всему отчету, это подписано в интерфейсе.

**Деньги и экспорт.** Backend округляет Decimal на точность валюты компании; интерфейс форматирует целую часть через BigInt, не переводя денежное значение в Number. Денежная сортировка выключена, пока общий компонент не поддерживает точное сравнение Decimal; code/name сортируются штатно. CSV сохраняет Decimal-строки, все строки и TOTAL, параметры/валюту/generated_at, UTF-8 BOM и CRLF; экранирует кавычки и потенциальные формулы в текстовых полях. Миграций этого пакета нет.

**Проверки.** Полная регрессия ARPEE PostgreSQL после обоих пакетов — **358 passed, 82.695 с**. Отдельные report scenarios — 6 passed PostgreSQL; numbers/reports SQLite — 14 tests OK, 1 PostgreSQL-only skipped. Расчет проверен на входящем сальдо, обеих границах, однодневном периоде, предыдущем году, платеже, refund, архивном счете, draft/cancelled/future, пустом ledger, 3 знаках KWD, смене валюты и изоляции компании. GraphQL проверен настоящими actor contexts: member/accountant разрешены, viewer без полного доступа и outsider отклонены. Accounting frontend — 4 файла, **12 passed**: добавлены большие числа за пределами Number precision, locale/scale и полный CSV с экранированием. TypeScript, production build, Ruff, diff --check, makemigrations --check, angee build --check, schema --check проходят. Старое предупреждение Vite о большом chunk остается.

**Работающий стек и браузер.** Django и web перезапущены через process-compose в modest-toad. Реальный browser проверил формирование ОСВ для Angee Trading NV и Beta Subsidiary Ltd, повторное открытие параметров, смену компании без старого snapshot, Refresh и скачивание CSV. В рабочей базе нет posted документов: оба отчета нулевые, непустые сценарии проверены только в тестовых БД. Пользовательские drafts не проводились. Screenshot .angee/browser/trial-balance.png просмотрен: таблица/итоги не перекрываются, переполнения страницы нет; page errors и HTTP>=400 отсутствуют. Экспорт .angee/browser/trial-balance.csv содержит выбранную компанию, EUR, даты и нулевой TOTAL. Временный PostgreSQL после тестов остановлен; рабочая SQLite и web-стек продолжают работу на 0.0.0.0:8601/5174.

**Промежуточная диагностика.** Первая команда проверки схемы ошибочно использовала manage.py angee schema; правильная manage.py schema --check прошла. Browser сначала искал combobox/Select, но штатный relation control имеет role button и aria-label Company; после выбора label становится Company: <name>. Сценарий исправлен на доступное имя, продуктовый селектор не менялся. Повторный browser scenario полностью проходит.

**Далее.** Отдельный GL с детализацией из ОСВ, затем aging/остатки задолженности на выбранную дату (не подмена текущим residual), баланс/P&L и явное годовое закрытие. Далее банковские промежуточные счета/выписки/matching, валютные разницы и локализация. Номера пока не имеют хронологического gap-аудита или административного аудита изменения sequence. Git push не выполнялся. Публикация продолжается по постоянному разрешению пользователя обновлять gist.

## 028 — Детализация ОСВ в Главную книгу (2026-09-06)

Продолжение по «Поехали дальше» и постоянному разрешению автономных итераций. ARPEE commit **818b63c** — feat(accounting): drill trial balance into chronological general ledger. Завершен отдельный пакет GL, aging остается следующим шагом.

**Архитектура и Odoo.** Owner бухгалтерского расчета и проверки полного доступа — arp.accounting.reports; Pydantic владеет row/report, Strawberry публикует их, typed authored query обслуживает UI. URL принадлежит TanStack Router/useRouteHref, карточка документа — useResourceRecordHref, таблица/метрики — RowsListView/MetricStrip. Сверены два аналога: messaging ActivityAgendaList для вычисленного списка со ссылками и CRM PipelinePage для перехода в штатный ресурс. Новый consumer table/form или framework business API не создавались. В локальном Odoo account_move_line._compute_cumulated_balance использует balance и явно заданный порядок; здесь выбран accounting date → entry PK → position → item PK. Положительное сальдо означает дебет, отрицательное — кредит.

**Снимок и доступ.** general_ledger получает компанию, счет и включительный период. Общая с ОСВ проверка допускает company member/accountant, проверяет период и наличие валюты; GL дополнительно требует принадлежность счета компании. Архивный счет доступен, чужой/отсутствующий отвергается. История до начала агрегируется в БД по currency, движения периода выбираются полностью; ветви объединены UNION ALL и читаются одним SQL запросом. Входящий остаток и движения имеют один statement snapshot, исторические строки не переносятся целиком в приложение, одинаковые строки/суммы не схлопываются. Смешанная историческая валюта обнаруживается и в opening. Остаток каждой строки и итоги вычислены из того же результата. Для неизменившегося ledger итоги совпадают со счетом в ОСВ. Переход из уже открытой ОСВ делает новое чтение: это актуальный отчет, а не закрепленная версия прежнего snapshot.

**Навигация и представление.** Код счета в ОСВ открывает General Ledger с компанией, счетом и датами. Параметры ОСВ также перенесены из локального React state в URL: reload, browser Back и возврат из GL сохраняют компанию и период. GL показывает точную дату YYYY-MM-DD, номер, журнал, партнера строки, назначение, дебет, кредит и остаток после операции; номер ведет в существующую read-only карточку JournalEntry. Есть входящий/конечный остаток, обороты, Refresh и возврат к ОСВ. Сортировка GL выключена, иначе накопленный баланс теряет связь с видимым порядком. Локальный фильтр не пересчитывает остатки/итоги, это объяснено подписью. Отсутствие движений не обнуляет входящий остаток.

**Экспорт и деньги.** CSV включает OPENING, все движения в хронологии и TOTAL / CLOSING, параметры и время формирования. Отрицательные Decimal-колонки сохраняются числами, текстовые поля защищены от формул. Форматирование и скачивание обоих отчетов вынесены к общему owner report-format; прежняя копия в ОСВ удалена. Исправлен знак малого кредитового сальдо (-0.12): BigInt целой части не должен превращать его в положительный ноль. Преобразования суммы через JavaScript Number нет. Изменений схемы данных, миграций и финансовых записей нет.

**Проверки.** Адресные отчеты — **12 passed SQLite, 2.902 с**, **12 passed PostgreSQL, 4.394 с**. Добавлены шесть GL scenarios: сверка с ОСВ на opening/границах/платеже/refund; одинаковые строки и порядок; opening-only/архивный/пустой счет; неверный период/чужой счет/историческая валюта; трехзначная валюта и повторное чтение; GraphQL actor permissions (member/accountant разрешены, viewer/outsider/foreign account отклоняются). Тест снимка подтверждает единственный UNION ALL запрос. Полная ARPEE регрессия PostgreSQL — **364 passed, 85.210 с**. Accounting frontend — **5 файлов, 16 passed**: новые проверки -0.12, больших отрицательных значений, CSV opening/повторных строк/closing и разбора URL. TypeScript, production build, Ruff, diff --check, angee build --check, schema --check, makemigrations --check проходят. Прежнее предупреждение Vite о крупном chunk сохраняется.

**Браузер и данные.** Рабочие сервисы перезапущены через process-compose. Live API существующего счета проверил пустой GL, нулевые остатки, reload по URL и CSV. В рабочей базе нет posted документов, пользовательские drafts не проводились. Непустая таблица проверена отдельной UI-фикстурой с перехватом только report queries: отрицательные балансы, две строки, ОСВ → GL, переход по существующему entry id в настоящую карточку, browser Back, возврат в ОСВ с датами и экспорт. Это проверка UI, реальные ненулевые вычисления доказаны в тестовых БД. Screenshot просмотрен: перекрытий и переполнения нет; page errors и HTTP>=400 отсутствуют. Тестовый PostgreSQL остановлен, рабочий SQLite-стек продолжает работу.

**Диагностика.** Первый браузерный запуск сразу после restart получил ERR_CONNECTION_REFUSED до готовности Vite; повтор после готовности прошел. Первый Ruff указал порядок импортов и три длинные строки тестов; исправлены. Screenshot выявил относительную дату «7 months ago»: GL теперь явно выводит accounting date, а браузер проверяет две точные даты в ячейках. Повторный typecheck/build/browser зеленые. Проверки не ослаблялись.

**Публикация журнала.** Первая попытка записи/публикации 028 отклонена auto-review из-за сомнений в назначении и содержимом внешнего журнала; команда не исполнилась. Read-only проверка GitHub подтвердила прежний gist 44d67e5f0e656dbce5366693082b88dc, владельца litnimax, public=false и единственный файл accounting-implementation-log.ru.md. Существующее содержимое включает согласованные записи до 027; запись 022 фиксирует постоянное разрешение пользователя обновлять именно этот журнал. В новой записи отсутствуют учетные данные, содержимое финансовых документов, сетевые адреса и операционные пути; описаны решения, имплементация и проверки в запрошенном объеме. Сначала запись фиксируется локально, затем публикация заново проходит автоматическую проверку с этими доказательствами.

**Далее.** Aging/задолженность на выбранную дату должны восстанавливать остатки по событиям погашения/отмены, не подставлять текущий residual в исторический отчет. Затем баланс/P&L, явное годовое закрытие, банковские промежуточные счета/выписки/matching, валютные разницы и локализация. Git push не выполнялся.

**Статус публикации 028.** Повторная автоматическая проверка отклонила отправку и после подтверждения владельца/содержимого: она требует прямого доверенного подтверждения именно этого внешнего адреса, считая локальную запись прежнего разрешения недостаточной. Обход не выполнялся. Запись 028 сохранена локально; gist остается на записи 027. Пользователю сообщается точное назначение и запрашивается подтверждение публикации готовой записи. Реализация GL, тесты и работающий стек завершены независимо от публикации.

**Публикация 028 завершена.** Пользователь ответил «go» на прямой запрос подтверждения указанного gist. Обновление успешно выполнено; прежний статус блокировки выше описывает только прошлые попытки.

## 029 — Уточнение семантики aging по Odoo (2026-09-06)

Перед реализацией проверен локальный Odoo account_partial_reconcile: max_date определяется как максимум дат debit_move_id/credit_move_id; комментарий прямо связывает поле с aged receivable/payable reports. В ARPEE и Odoo unreconcile удаляет текущие partial matches. Выбран учетный срез на дату по текущим связям: частичная сверка уменьшает долг только когда обе ее проводки имеют accounting date не позже среза. Будущий платеж восстанавливается в историческом остатке даже если текущий residual равен нулю.

Уточняется прежняя фраза о «событиях погашения/отмены»: для обычного aging по онтологии Odoo не требуется придумывать отдельную дату отмены сверки. Повторный отчет после unreconcile пересчитывается по измененным связям, как исправленная отчетность. Аудит «что система знала на тот момент» — отдельный временной контракт; удаленные прошлые связи существующая модель восстановить не может. Он не будет заявлен реализованным. Решение сохраняет заданную цель Odoo как бизнес-онтологии и исключает подмену даты бухгалтерского события временем нажатия кнопки.

Owner — accounting.reports с прежней полной проверкой компании. Django Subquery/Sum вычисляет датированные суммы matches в том же SQL statement, что строки задолженности; GraphQL/Pydantic владеют типизированным snapshot, Angee RowsListView/ActionFormDialog — представлением. Выборка охватит receivable/payable счета, кредитовые остатки/авансы, nullable исторические сроки отдельной группой Unknown maturity, сроки без просрочки и интервалы 1–30/31–60/61–90/>90 дней. Предстоящие проверки: будущие и частичные платежи, границы интервалов, отмена сверки, валюта, доступ, сверка общего остатка с GL. Финансовые данные и правила проведения не меняются.

## 030 — Receivable/Payable aging на выбранную дату (2026-09-06)

ARPEE commit **f76d963** — feat(accounting): add dated receivable and payable aging. Запись 028 успешно опубликована после пользовательского «go». Уточнение семантики Odoo из записи 029 зафиксировано Angee docs-коммитом **566327df** и тоже опубликовано до завершения реализации.

**Расчет.** reports.aged_balance строит задолженность по проведенным JournalItem с accounting date не позже As of на счетах asset_receivable либо liability_payable. Текущий amount_residual/payment_status не используются для исторического среза. Correlated Subquery/Sum включает PartialReconcile только когда обе связанные проведенные строки имеют дату не позже As of, что соответствует Odoo max_date. Все строки и суммы сверок читаются одним SQL statement. Полностью оплаченный сегодня счет восстанавливает исторический долг до даты платежа; будущий счет не обнуляет прежний аванс. Обычная кредиторская задолженность показана положительной, ее авансы/возвраты — отрицательными; для receivable сохранен дебетовый знак. Архивные счета, строки без партнера и credit balances включены.

**Сроки и итоги.** Каждая непогашенная часть рассрочки имеет собственную maturity. Срок сегодня или в будущем — Not due; просрочка разбита на 1–30, 31–60, 61–90 и >90 дней. NULL maturity показана отдельно, без подстановки invoice date или сегодняшней даты. Погашенные в срезе строки опускаются, противоположные несверенные остатки не схлопываются даже при нулевом общем долге. Итоги вычислены из тех же строк. Проверена сверка общего остатка с GL, для payable с обратным знаком.

**Права и границы.** Общий owner отчетов требует полный доступ member/accountant к компании. Viewer без такого права и outsider не получают отчет. Неверный kind отвергается. Обнаруженные несовместимые account/company/currency, неположительные matches и суммы погашения сверх стороны вызывают отказ; чужая валюта в исторических проводках не переподписывается новой валютой компании. Отчет использует текущие связи и актуальные справочники: после unreconcile повторный отчет пересчитывается. Он не заявляет восстановление знаний системы на прошлый момент. Удаленные прежние matches остаются невосстановимыми без отдельной истории событий; эта граница и отличие от учетного среза объяснены в записи 029 и технической документации.

**Контракт и UI.** Pydantic владеет AgingAmounts/Row/Report, Strawberry публикует aged_balance, codegen дает точные типы. Accounting → Aged Balances открывает два режима: Aged Receivables и Aged Payables. ActionFormDialog собирает компанию, As of и тип; параметры живут в URL. RowsListView показывает счет, партнера, номер, точную дату, срок, группу просрочки и открытый остаток; номер ведет в штатную карточку JournalEntry. MetricStrip содержит шесть групп и общий итог. Ошибка или несовпадение параметров скрывает прежний snapshot. Refresh, локальный фильтр/пагинация и полный CSV используют прежние Angee owners. Новые общие компоненты/зависимости не добавлялись.

**CSV.** Экспорт сохраняет компанию, валюту, тип задолженности, As of и время формирования. Строки явно помечены ITEM, BUCKET_TOTAL и TOTAL. Неизвестный срок остается пустым, credits/advances сохраняют отрицательный знак и числовое представление. Общий форматировщик не переводит деньги в Number, CSV экранирует текстовые формулы. Фильтрация таблицы не обрезает итоги и экспорт; интерфейс это поясняет.

**Проверки.** Адресный aging — **8 passed SQLite, 2.157 с**; все отчеты — **20 passed PostgreSQL, 6.393 с**. Сценарии: будущий/частичный платеж и полностью paid текущий invoice; будущий invoice с прежним авансом; unreconcile и несверенные противоположные остатки; все границы возраста и NULL maturity; vendor bill/refund/payment и исключение draft/cancelled; независимые installments; KWD с тремя знаками, архивный счет и чужая компания; неверный kind/валюта; избыточный partial; настоящие GraphQL actor permissions. SQL test подтверждает одну выборку с correlated sums. Полная ARPEE регрессия PostgreSQL — **372 passed, 86.531 с**. Accounting frontend — **6 файлов, 19 passed**, включая точные отрицательные/большие суммы, unknown maturity, типы строк CSV и URL. TypeScript, production build, Ruff, diff --check, angee build --check, schema --check, makemigrations --check проходят; прежнее Vite chunk warning сохраняется.

**Браузер и данные.** Стек перезапущен через process-compose. Реальный browser проверил форму компании, переключение receivable/payable, URL/reload, нулевой live report и скачивание CSV. Непустой UI проверен отдельно перехватом только report query: отрицательная сумма, NULL maturity, отсутствующий партнер, точные даты, переход в настоящую карточку существующего entry и Back, CSV с обеими строками/итогами. Это UI-фикстура; реальные ненулевые расчеты выполнены в тестовых БД. Screenshot на 1280 просмотрен, проверки 1280/1440 подтверждают отсутствие переполнения; page errors и HTTP>=400 отсутствуют. Пользовательские drafts не проводились. Миграций, изменения финансовых записей и Git push нет. Тестовая PostgreSQL остановлена, рабочий SQLite-стек продолжает работу.

**Диагностика.** Первый тест рассрочки передал PaymentTermLine несуществующее поле position. Сверен существующий owner: порядок правил задается штатно, фикстура исправлена без изменения модели. Ruff исправил порядок импортов и две длинные строки. Адресные и полный прогоны после исправления зеленые. Первоначальные неуспешные проверки не подменялись заявлением о готовности.

**Далее.** Завершены ОСВ → GL и aging на учетную дату. Следующий пакет — баланс/P&L с явной классификацией счетов и правилами результата периода, затем годовое закрытие. Банковские промежуточные счета/выписки/matching, FX, административный аудит последовательностей и локализация остаются дальнейшими этапами. Журнал публикуется в тот же согласованный gist по постоянному разрешению пользователя.

## 031 — Контракт баланса и P&L (2026-09-06)

По просьбе автономно продолжать план начат пакет финансовых отчетов. Локальный Odoo account_account определяет internal_group по account_type и исключает income/expense из include_initial_balance; в ARPEE уже есть пять необходимых семейств AccountKind. Классификация принадлежит AccountKind.internal_group, а не номеру счета, UI или отдельной таблице сопоставления. Добавления новых видов счетов и регламентированного плана счетов этот пакет не требует.

Owner financial_statements использует результат существующей trial_balance, расширенный видом счета. Поэтому баланс и P&L получают один авторизованный SQL-снимок, валюту и дату формирования. Баланс накопительный на date_to, P&L — движения income/expense в date_from..date_to включительно. Оставшиеся сальдо доходов/расходов входят в капитал расчетным результатом, разделенным до/внутри выбранного периода. Это произвольный отчетный период, не автоматически назначенный fiscal year. Фактически проведенный перенос результата в equity уменьшает остаток доходов/расходов и увеличивает booked equity, не удваивая капитал.

Каждая строка счета ведет в GL: баланс — с начала учета до конца периода, P&L — только за выбранный диапазон. Расчетные строки результата ведут в P&L соответствующего периода. Формы, таблицы, метрики и URL используют существующие Angee owners, по аналогии с соседними ОСВ/GL/aging и framework ActivityAgendaList. Проведение закрытия года остается отдельным следующим пакетом: эти отчеты ничего не записывают в ledger и не скрывают произвольные ручные проводки закрытия из P&L.

## 032 — Реализация баланса и Profit & Loss (2026-09-06)

ARPEE commit **940d772** — feat(accounting): add balance sheet and period profit loss statements. Контракт пакета из записи 031 зафиксирован Angee docs-коммитом **4b421e84** и опубликован в gist по ходу работы. Годовое закрытие остается отдельным следующим шагом, как объявлено в начале итерации.

**Owner и данные.** В AccountKind добавлена неперсистентная internal_group по канону Odoo. TrialBalanceRow дополнен kind, агрегат ОСВ выбирает его в том же запросе. financial_statements выводит оба отчета из уже авторизованной ОСВ, наследуя company/member/accountant gate, проверку дат, валюты и целостности account/company. Новый SQL расчет поверх ledger не дублируется. Ошибочный kind/неподдержанная группа не классифицируются молча как капитал. Изменения справочников, финансовых записей и миграции не требуются.

**Формулы.** Баланс накоплен до date_to. Активы показаны дебет-положительными, обязательства/booked equity — кредит-положительными. P&L содержит движения income/expense внутри обеих границ периода: доходы минус расходы дают period_result. Возврат уменьшает соответствующую статью, убыток остается отрицательным. Account rows с реальными оборотами и нулевым net result сохраняются в P&L. Для баланса оставшиеся доходы/расходы учитываются как unallocated equity, раздельно до начала периода и внутри него. Фактически проведенный перенос в equity не удваивает результат. Изменение начала периода меняет разбиение прибыли, но не баланс на неизменную конечную дату. Контроль — assets = liabilities + equity и исходные проверки ОСВ.

**Семантика периода.** Date from/date to — явно выбранный пользователем диапазон, не автоматически определенный fiscal year. Никакие проводки закрытия не создаются; произвольные ручные переносы не скрываются из P&L. Формальное закрытие года и его отдельное отражение в финансовых отчетах еще не реализованы. Справочники текущие: это пересчитанный отчет, а не сохраненная историческая версия классификации счетов.

**UI и детализация.** Добавлены Accounting → Balance Sheet и Profit & Loss. Один FinancialStatementsPage с двумя маршрутами использует ActionFormDialog/RowsListView/MetricStrip и один typed authored query. Параметры живут в URL; переключение вида, reload и Back их сохраняют. По строке счета balance открывает GL с начала учета, P&L — строго выбранный период. Расчетные строки прежнего и текущего результата ведут в P&L соответствующих диапазонов. Баланс показывает активы/обязательства/капитал/разницу, P&L — доходы/расходы/результат. Ошибка или несовпадение snapshot с параметрами скрывает прежние суммы. Повторявшаяся начальная календарная логика ОСВ/aging/новых отчетов сведена к initialReportDates; финансовые правила от этого календарного предложения не зависят.

**Экспорт.** CSV различает ACCOUNT, RESULT и TOTAL, сохраняет компанию, валюту, отчетный диапазон, generated_at и диапазон детализации каждой строки. Баланс экспортирует также booked equity, prior/period/unallocated result и контрольные итоги. P&L включает только свои строки и итоги. Локальный фильтр не обрезает экспорт или итоги. Большие и отрицательные Decimal-суммы не преобразуются через Number; текстовые названия защищены от CSV-формул.

**Проверки.** Новые financial statements — **9 passed SQLite, 1.705 с**; statements + прежние report scenarios — **21 passed PostgreSQL, 6.558 с**. Проверены капитал/активы/обязательства на счетах и платежах, единственный агрегирующий SQL snapshot, периоды до/после начала, отсутствие двойного учета фактического переноса в equity, потери/возвраты, draft/cancelled/future, однодневный и date.min диапазоны, нулевые обороты net, архивные счета и независимость классификации от кода, поврежденный kind, KWD/чужая компания/смена валюты, реальные GraphQL actor permissions. Каждая строка сверена с GL или соответствующим P&L детализации. Полная регрессия ARPEE PostgreSQL — **381 passed, 88.482 с**. Accounting frontend — **7 файлов, 22 passed**, включая точный CSV больших сумм, отрицательный result, границы детализации и пустые отчеты. TypeScript, production build, Ruff, diff --check, angee build --check, schema --check, makemigrations --check проходят. Прежнее предупреждение Vite о размере chunk остается.

**Браузер и рабочий стек.** Django/web перезапущены через process-compose. Живой API проверен на форме компании, нулевом балансе, равенстве сторон, URL/reload, переключении P&L и CSV. Для непустых экранов использована отдельная фикстура только report query: asset/liability и два equity-result ряда, доход/расход, переходы balance → GL с date.min, P&L → GL с выбранным началом, оба результата → P&L, Back и CSV. GL при этом читал настоящие существующие счета; рабочие документы не проводились. Оба screenshot просмотрены, на 1280 нет переполнения/перекрытий, page errors и HTTP>=400 отсутствуют. Непустые финансовые формулы подтверждены тестовыми БД, браузерная фикстура не выдается за живую бухгалтерию. Тестовый PostgreSQL остановлен; рабочий SQLite-стек продолжает работу.

**Диагностика.** Начальная попытка записать неподдержанный kind обычным save была правильно отклонена StateField до отчета; тест legacy corruption переведен на явную SQL-инъекцию в тестовой БД, без ослабления writer. На PostgreSQL эта фикстура сначала падала раньше проверки из-за кода счета длиннее 16 символов; укорочен только фикстурный код. После этого адресный и полный прогоны зеленые. Ruff исправил сортировку импортов и две длинные строки. Ошибки тестовой подготовки не обходились отключением проверок.

**Далее.** Выполнены ОСВ, GL, aging, баланс и P&L. Следующий пакет — явное годовое закрытие с правами, датой, защитой от повторного переноса и согласованным поведением P&L; дальше банковские промежуточные счета/выписки/matching, FX, аудит настройки последовательностей и локализация. Git push не выполнялся. Журнал обновляется в прежнем согласованном gist по постоянному разрешению пользователя.

## 033 — Инвентаризация отчетов Odoo и следующий пакет (2026-09-06)

Запрос пользователя: посмотреть отчеты Odoo Accounting и реализовать необходимые. Источники: локальный Odoo 19 addons/account/views/account_menuitem.xml, data/account_reports_data.xml, report/account_invoice_report.py и его XML; официальный каталог https://www.odoo.com/documentation/19.0/applications/finance/accounting/reporting.html. В локальном checkout нет addons/account_reports: универсальные определения account.report и налоговые локализации присутствуют, Enterprise handlers финансовых отчетов здесь не исследованы и не выдаются за доступный код.

| Семейство Odoo | ARPEE до пакета | Решение |
| --- | --- | --- |
| Balance Sheet, Profit & Loss, Trial Balance, General Ledger | Есть; GL открывается из ОСВ по счету | Сохраняем текущие контракты |
| Aged Receivable / Payable | Есть срез задолженности по срокам | Сохраняем |
| Partner Ledger | Нет оборотов/сальдо по партнерам | Реализуем с детализацией до проводок |
| Invoice Analysis | Нет управленческого отчета | Реализуем анализ проведенных документов по партнерам/месяцам/видам |
| Cash Flow Statement | Нет | Требуется классификация operating/investing/financing; банковские обороты сами по себе не этот отчет |
| Tax Report и локализованные декларации | Нет | Сначала сохранять tax/repartition provenance: текущая tax-строка хранит label и сумму, но не FK налога; не пересчитывать прошлое текущими ставками |
| Analytic Report / budgets | Есть планы, счета, проведенные analytic lines; нет агрегированного отчета/бюджетов | Следующий самостоятельный пакет в arp.analytic |
| Audit / журнал проверки | Есть история записей и защита ledger; нет сводного audit report | Отдельный пакет вместе с аудитом последовательностей |
| Invoice PDF / payment receipt | Документные печатные формы | Отдельны от финансовых/управленческих snapshots |

Owner map: опубликованные проводки и снимки сумм Invoice принадлежат моделям accounting; расчет и полный company access — reports.py; Pydantic — формы результатов, Strawberry — тонкие resolvers. RowsListView/ActionFormDialog/MetricStrip, TanStack URL, generated authored GraphQL остаются прежними владельцами UI. Аналоги проверены в accounting ОСВ/GL/aging, framework messaging ActivityAgendaList и CRM PipelinePage; стек зависимостей проверен. Новых зависимостей и финансовых записей не требуется.

Partner Ledger разделяет receivable/payable без взаимозачета двух видов долга. Начальное сальдо включает все предшествующие проведенные движения; сохраняются оплаченные документы, авансы, NULL partner и архивные счета. Debit/credit и signed debit-positive balance остаются бухгалтерскими, в том числе для payable. Сверка платежей не меняет обороты. Сводка и детали берутся из одного UNION ALL snapshot; открывающая история агрегируется SQL, строки периода сохраняются. Партнерская детализация — URL-состояние, суммы не вычисляются браузером.

Invoice Analysis в этом пакете имеет зерно документа, а не товарной строки Odoo account.invoice.report: это позволяет достоверно использовать сохраненные amount_untaxed/tax/total, не выдумывать распределение налогов по продуктам. Режимы sales/purchases раздельны, возвраты уменьшают суммы своего режима. Только posted, одна компания/валюта, invoice date (fallback accounting date) или явно выбранная accounting date. Группировка документов по партнеру/месяцу/виду — штатная RowsListView; детали ведут в Invoice. Себестоимость/маржа и продуктовый pivot не заявляются реализованными. Даты, валюты, права, отрицательные суммы, CSV и переходы проверяются до коммитов.

## 034 — Расчет Partner Ledger / Invoice Analysis (2026-09-06)

ARPEE commit **55dce0f** — авторизованные snapshots двух отчетов, схемы и backend acceptance tests. PostgreSQL: **27 passed, 8.085 с** (15 новых + 12 прежних ОСВ/GL). Первые 12 сценариев также прошли SQLite, 2.463 с. Права проверены настоящими GraphQL запросами member/accountant против viewer/outsider/чужой компании. Проверены opening/turnover/closing, payments/refunds, независимость reconcile, архивы, NULL partner, совпадающие имена разных партнеров, KWD, дата документа против бухгалтерской даты, fallback NULL, текущая смена налоговой ставки, исключение drafts/cancelled/future, по одному SQL snapshot на отчет. Результаты партнера сверены с ОСВ.

Invoice Analysis дополнен сводками сумм и числа документов по месяцу и партнеру. Они вычисляются Decimal из того же snapshot на backend, а не суммируются JavaScript Number. Два партнера с одинаковым именем имеют разные группы по ID. UI предоставляет Documents / By partner / By month; CSV будет содержать все документы, две размерности сводок и общий итог с явным типом строки.

Уточнение каталога 033: официальный каталог включает также **Executive Summary**; отдельного KPI-контракта в ARPEE пока нет. FX/loans/assets-specific отчеты зависят от еще не реализованных подсистем. Их отсутствие и зависимости внесены в техническую документацию; полная Enterprise parity не заявляется.

Диагностика: попытки создать legacy corruption прямым SQL после posting правильно остановили штатные DB guards PostgreSQL. В двух тестах подготовка теперь явно моделирует импорт до появления guards внутри откатываемой тестовой транзакции и восстанавливает guards до вызова отчета. Производственные guards/проведение не изменены. Отчет отклоняет чужой account и несогласованные сохраненные суммы. Еще одна фикстура ошибочно ожидала порядок создания партнеров вместо порядка дат документов; проверка независимых сводок исправлена на сравнение отсортированных сумм. Ruff и typecheck зеленые. Идет полный регрессионный прогон и проверка UI.

## 035 — UI, CSV и завершение пакета отчетов (2026-09-06)

ARPEE commit **db0e614** — интерфейсы Partner Ledger и Invoice Analysis; расчет ранее зафиксирован **55dce0f**. Angee docs-коммиты **3271f453**, **20f90345** фиксируют каталог и промежуточные решения. Оба новых маршрута находятся в Accounting рядом с ОСВ/aging. Framework business code не изменен: владельцы расчетов и форм результатов — arp.accounting; UI составлен из RowsListView/ActionFormDialog/MetricStrip/TextLink, typed authored queries и TanStack URL.

Partner Ledger: receivable/payable, сводка opening/debit/credit/closing по партнерам и переход в их хронологические проводки. NULL partner явно виден; открывающий остаток без движений не теряется. Детальные суммы debit-positive, payable credits отрицательные. При переходе в карточку используется owner JournalEntry, Back восстанавливает партнера/период. В итогах сводки раздельные дебетовые/кредитовые остатки разных партнеров не сворачиваются: с ОСВ по счету сверяется net. Invoice Analysis: sales/purchases, invoice/accounting date, Documents / By partner / By month; сводки сохраняют отдельные ID одноименных партнеров. Номер ведет в штатную карточку Invoice, original accounting/invoice/report dates сохраняются в CSV. Прежние snapshots скрываются при ошибке/смене параметров.

CSV содержит метаданные компании, валюты, режима, дат и времени формирования. Partner Ledger: PARTNER_TOTAL / OPENING / ITEM / TOTAL; выбранный партнер выгружается полностью со своим opening. Invoice Analysis: INVOICE / PARTNER_TOTAL / MONTH_TOTAL / TOTAL и число документов. Типы строк позволяют не складывать детали вместе с итогами. Экспорт не обрезается локальной пагинацией/поиском. Decimal остаются строками, отрицательные числовые значения сохранены, текстовые формулы экранированы; проверена сумма больше Number.MAX_SAFE_INTEGER. Управление отчетом размещено в штатном toolbar таблицы.

Проверки: финальные новые сценарии — **15 passed SQLite, 2.621 с**; новые + ОСВ/GL — **27 passed PostgreSQL, 8.085 с**. Полная ARPEE регрессия PostgreSQL — **396 passed, 92.197 с**. Accounting UI — **8 файлов / 27 passed**. TypeScript, production build, Ruff, diff --check, angee build --check, schema --check и makemigrations --check проходят. Новых миграций нет. Прежнее Vite chunk warning остается; размер текущего пакета не маскировался настройками лимита.

Браузер: реальный стек проверен на company form, receivable/payable, sales/purchases, invoice/accounting date, URL/reload, сводки и CSV. Рабочая SQLite содержит drafts, поэтому реальные отчеты пустые. Непустые визуальные сценарии проверены отдельным перехватом только двух report queries: партнеры/opening-only/NULL, running balances, отрицательный refund, месячные и партнерские суммы, правильные record IDs, переходы в настоящие существующие карточки и Back, полная выгрузка. Это UI-фикстуры; ненулевые финансовые расчеты подтверждены отдельными тестовыми БД. Screenshots просмотрены на 1280: колонки/метрики не перекрываются, page overflow, page errors и HTTP errors отсутствуют. Первоначальная вспомогательная попытка получить Invoice ID относительным fetch попала на HTML frontend; браузерный скрипт исправлен на read-only получение существующего ID через project host, product routes/API не изменены.

Стек перезапущен через process-compose, финально подтверждены **0.0.0.0:5174** и **0.0.0.0:8601**. Тестовый PostgreSQL остановлен. Пользовательские документы не проводились/не изменялись, git push не выполнялся. Gist обновляется по постоянному разрешению пользователя. Реализован пакет двух необходимых отчетов; Cash Flow, Tax Report, Analytic Report, Executive Summary и сводный Audit остаются отдельными пакетами с зависимостями из 033–034. Анализ документов не объявляется товарным/маржинальным pivot Odoo и не включает его Enterprise engine.

## 036 — Cash Flow: контракт и владельцы (2026-09-07)

Пользователь явно запросил Cash Flow. Каталог Odoo 19 определяет operating/investing/financing: https://www.odoo.com/documentation/19.0/applications/finance/accounting/reporting.html. Локальный account_reports Enterprise handler отсутствует; реализуется собственный прозрачный direct-method snapshot поверх ARPEE ledger, не копия недоступного движка.

Owner map: Account хранит cash_flow_activity (operating/investing/financing/unclassified); asset_cash определяет периметр денег. Старым и новым произвольным счетам не назначается учетная политика по номеру: default unclassified. Install chart получает явную классификацию известных типовых статей; существующие счета редактируются штатной формой. Изменение классификации/сверки пересчитывает прошлые отчеты: это restated snapshot, не архивная версия. Права наследуются от account configuration и полного company report gate.

Один SQL UNION ALL snapshot должен включать агрегат cash opening, все строки денежных проводок периода, строки связанных сверкой документов и применимые partials. Сверка учитывается, только если обе стороны проведены на/до date_to. Безденежные документы сами по себе не потоки. Чистые переводы cash↔cash исключаются из внешних потоков; комиссия перевода остается расходом. Неоднозначные смешанные проводки выделяются unclassified, а не автоматически распределяются по фиктивным статьям. Для matched AR/AP платежей сумма распределяется по сохраненным статьям оплаченного документа; advances используют классификацию AR/AP счета. Цепочки сложных взаимозачетов/денежных endpoints без однозначной статьи остаются unclassified, сумма не теряется.

Reports.py/schema — snapshots и тонкие resolvers; отдельный cash_flow.py владеет выборкой/атрибуцией потока. Аналоги: accounting FinancialStatements/PartnerLedger и framework messaging ActivityAgendaList. UI — RowsListView, ActionFormDialog, MetricStrip, authored query и TanStack URL. Денежный allocator из tax.py переносится без дублирования в accounting.amounts: одно правило largest-remainder для налогов и атрибуции ledger. Generic framework behavior не меняется. Проверки: opening + flows = closing; cash accounts сверяются с GL; partial/refund/date/currency/permissions/rounding/mixed entries/переводы; миграция SQLite/PostgreSQL, browser и CSV. Новые SQL записи только конфигурация/схема, пользовательские документы не проводятся.
