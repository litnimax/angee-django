# Angee addon manifest. The declarative contract hatch-angee compiles into
# the package's [project] + entry points; see hatch-angee's native AddonManifest.
[addon]
name = "angee.resources"
description = "Tiered data import/export — load demo and seed data from declarative resource files"
keywords = ["resources", "import", "data"]
category = "Foundation"
depends_on = ["angee.base"]
dependencies = [
  "django-import-export>=4.4",
  "pyyaml>=6",
  "tablib>=3",
]
